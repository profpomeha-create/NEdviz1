# Dashboard - Аналитический дашборд недвижимости

React + TypeScript дашборд с 12 аналитическими отчетами по недвижимости.

## 📋 О проекте

Дашборд для анализа данных о недвижимости с интерактивными графиками, KPI-карточками и детальными таблицами. Проект полностью адаптирован для работы на устройствах от мобильных телефонов (375px) до десктопов (1920px).

## ✨ Что выполнено

### Основной функционал
- ✅ **12 аналитических отчетов** с различными метриками и визуализациями
- ✅ **Полностью адаптивный дизайн** (375px - 1920px) с использованием CSS Media Queries
- ✅ **Мультиселект фильтры** для выбора нескольких значений одновременно
- ✅ **KPI карточки** с централизованным отображением ключевых метрик
- ✅ **Интерактивные графики** на базе Chart.js (линейные, столбчатые, круговые)
- ✅ **Детальные таблицы** с данными, адаптированные для мобильных устройств
- ✅ **Маршрутизация** через React Router для навигации между отчетами
- ✅ **Деployment** на GitHub и Vercel

### UI/UX компоненты
- ✅ Кастомный компонент `MultiSelect` для множественного выбора
- ✅ Компонент `KPICard` для отображения ключевых показателей
- ✅ Компонент `ChartWrapper` для унификации работы с графиками
- ✅ Компонент `DataTable` для таблиц с данными
- ✅ Компонент `UnitToggle` для переключения единиц измерения
- ✅ Боковое меню навигации с активными состояниями

### Адаптивность
- ✅ **Desktop** (1280px+): полноэкранный вид с боковой панелью
- ✅ **Tablet** (768px-1280px): адаптация размеров шрифтов и отступов
- ✅ **Mobile** (375px-768px): вертикальная компоновка, уменьшенные шрифты, горизонтальный скролл для таблиц
- ✅ Оптимизация графиков для разных размеров экрана
- ✅ Адаптивные фильтры с вертикальным расположением на мобильных

## 🛠 Как это сделано

### Архитектура
Проект построен на **React 19** с использованием **TypeScript** для типобезопасности. Сборка выполняется через **Vite** для быстрой разработки и оптимизированной production-сборки.

### Структура проекта
```
dashboard/
├── src/
│   ├── components/ui/      # Переиспользуемые UI компоненты
│   │   ├── ChartWrapper.tsx    # Обертка для Chart.js графиков
│   │   ├── DataTable.tsx       # Таблица данных
│   │   ├── KPICard.tsx         # KPI карточка
│   │   ├── MultiSelect.tsx     # Мультиселект фильтр
│   │   └── UnitToggle.tsx      # Переключатель единиц
│   ├── layout/             # Компоненты макета
│   │   ├── Layout.tsx          # Основной layout
│   │   └── TopTabs.tsx         # Навигационное меню
│   ├── pages/              # Страницы отчетов (12 страниц)
│   │   ├── SummaryPage.tsx     # Сводный отчёт
│   │   ├── MarketPage.tsx      # Параметры рынка
│   │   ├── ExpressPage.tsx     # Экспресс-анализ
│   │   ├── DynamicsPage.tsx    # Динамика (основная)
│   │   ├── DynamicsPage41.tsx  # Динамика 4.1
│   │   ├── DynamicsPage42.tsx  # Динамика 4.2
│   │   ├── DynamicsPage43.tsx  # Динамика 4.3
│   │   ├── DynamicsPage44.tsx  # Динамика 4.4
│   │   ├── CompetitivePage.tsx # Конкурентный анализ
│   │   ├── MortgagePage.tsx    # Ипотека
│   │   ├── TodayPage.tsx       # Сегодня в продаже
│   │   ├── ForecastPage.tsx    # Перспектива
│   │   ├── WashoutPage.tsx     # Вымываемость
│   │   ├── NewLotsPage.tsx     # Новые лоты
│   │   ├── MarketingPage.tsx   # Маркетинг
│   │   └── CommercialPage.tsx  # Коммерческая недвижимость
│   ├── lib/                # Утилиты и конфигурация
│   │   ├── chartConfig.ts      # Конфигурация Chart.js
│   │   ├── data.tsx            # DataProvider и моки данных
│   │   └── types.ts            # TypeScript типы
│   ├── utils/              # Вспомогательные функции
│   │   └── formatters.ts       # Форматирование чисел/даты
│   ├── App.tsx             # Главный компонент с роутингом
│   ├── main.tsx            # Точка входа
│   └── index.css           # Глобальные стили и медиа-запросы
├── public/                 # Публичные файлы
└── package.json            # Зависимости проекта
```

### Ключевые технические решения

#### 1. Адаптивный дизайн
Использованы **CSS Media Queries** для следующих breakpoints:
- `375px` - Мобильные телефоны (маленькие)
- `480px` - Мобильные телефоны (средние)
- `640px` - Мобильные телефоны (большие)
- `768px` - Планшеты
- `1024px` - Небольшие десктопы
- `1280px` - Десктопы
- `1440px` - Большие десктопы
- `1920px` - Очень большие экраны

Для адаптивности применены:
- Flexbox и CSS Grid с адаптивными `gap` и `min-width`
- Динамические размеры шрифтов (`font-size`)
- Адаптивные отступы (`padding`, `margin`)
- Горизонтальный скролл для таблиц на мобильных (`overflow-x: auto`)
- Вертикальная компоновка фильтров на маленьких экранах (`flex-direction: column`)

#### 2. Мультиселект фильтры
Реализован кастомный компонент `MultiSelect` с возможностью:
- Выбора нескольких опций одновременно
- Выбора всех опций одной кнопкой
- Отображения выбранных значений в виде чипсов с возможностью удаления
- Закрытия при клике вне компонента (через `useRef` и `useEffect`)

#### 3. Работа с графиками
- Использован `react-chartjs-2` как обертка над Chart.js
- Компонент `ChartWrapper` обеспечивает:
  - Единообразный API для всех типов графиков
  - Адаптивные размеры через CSS
  - Мемоизацию опций через `useMemo`
  - Настройки легенды и tooltip для мобильных устройств

#### 4. State Management
- Использован Context API (`DataProvider`) для глобального состояния данных
- Локальное состояние через React Hooks (`useState`, `useMemo`) на каждой странице
- Мемоизация вычислений для оптимизации производительности

#### 5. Таблицы
- Адаптивные таблицы с горизонтальным скроллом на мобильных
- Sticky header для удобства работы с большими таблицами
- Оптимизированные размеры ячеек и шрифтов для разных экранов
- Использование `table-layout: fixed` для предсказуемой ширины колонок

## 📊 Отчеты

1. **Сводный отчёт** - Общая статистика по всем показателям
2. **Параметры рынка** - Анализ рыночных параметров с фильтрацией
3. **Экспресс-анализ** - Быстрый анализ ключевых метрик
4. **Динамика** - Основной отчет по динамике показателей
   - 4.1 Динамика (детальный подотчет)
   - 4.2 Динамика (детальный подотчет)
   - 4.3 Динамика (детальный подотчет)
   - 4.4 Динамика (детальный подотчет)
5. **Конкурентный анализ** - Сравнительный анализ конкурентов
6. **Ипотека** - Статистика по ипотечным предложениям
7. **Сегодня в продаже** - Актуальные предложения с детализацией по этажам
8. **Перспектива** - Прогнозные данные
9. **Вымываемость** - Анализ выбытия объектов из продажи
10. **Новые лоты** - Статистика по новым поступлениям
11. **Маркетинг** - Маркетинговые метрики и аналитика
12. **Коммерческая недвижимость** - Отдельный раздел для коммерческой недвижимости

## 🚀 Технологический стек

### Core
- **React 19.1.1** - UI библиотека
- **TypeScript 5.9.3** - Типизация кода
- **Vite 5.4.10** - Сборщик и dev-сервер

### Роутинг
- **React Router DOM 7.9.5** - Клиентская маршрутизация

### Визуализация данных
- **Chart.js 4.5.1** - Библиотека для графиков
- **react-chartjs-2 5.3.1** - React обертка для Chart.js

### Утилиты
- **clsx 2.1.1** - Условное объединение CSS классов

### Инструменты разработки
- **ESLint 9.36.0** - Линтер кода
- **TypeScript ESLint 8.45.0** - ESLint правила для TypeScript
- **Vite Plugin React 5.0.4** - Плагин для поддержки React в Vite

## 📦 Установка и запуск

### Локальная разработка

```bash
# Установка зависимостей
cd dashboard
npm install

# Запуск dev-сервера
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Сборка для production

```bash
cd dashboard
npm run build
```

Собранные файлы будут в директории `dashboard/dist`

### Предпросмотр production-сборки

```bash
cd dashboard
npm run preview
```

## 🌐 Деплой

### GitHub
Репозиторий: `https://github.com/profpomeha-create/NEdviz1.git`

### Vercel
Проект автоматически настроен для деплоя на Vercel через `vercel.json`:

```json
{
  "buildCommand": "cd dashboard && npm run build",
  "outputDirectory": "dashboard/dist",
  "devCommand": "cd dashboard && npm run dev",
  "installCommand": "cd dashboard && npm install",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
```

Процесс деплоя:
1. Подключите репозиторий к Vercel
2. Vercel автоматически определит настройки из `vercel.json`
3. При каждом push в `main` ветку происходит автоматическая сборка и деплой

## 📝 Особенности реализации

### Производительность
- Мемоизация компонентов через `memo()` для предотвращения лишних рендеров
- Мемоизация вычислений через `useMemo()` для дорогих операций
- Ленивая загрузка данных через Context API

### Доступность
- Семантическая HTML разметка
- ARIA атрибуты для навигации
- Правильная структура заголовков

### Код качество
- TypeScript для статической типизации
- ESLint для проверки качества кода
- Компонентная архитектура для переиспользования

## 🔄 Обновление проекта

Для обновления проекта на GitHub и Vercel:

```bash
# Добавить изменения
git add .

# Закоммитить
git commit -m "Описание изменений"

# Отправить на GitHub
git push origin main
```

Vercel автоматически обнаружит новый коммит и пересоберет проект.

## 📄 Лицензия

Проект создан для внутреннего использования.

---

**Разработано с использованием React 19, TypeScript и Chart.js**
